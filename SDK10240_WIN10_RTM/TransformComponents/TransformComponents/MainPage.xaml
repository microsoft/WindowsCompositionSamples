<Page
    x:Class="TransformComponents.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TransformComponents"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid x:Name="TopGrid" Grid.Row="0">
            <Grid x:Name="Placeholder" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        
        <Grid x:Name="BottomGrid" Grid.Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Margin="10" Click="OpacityToggle_Click" Content="Toggle Opacity"/>
                <Button Margin="10" Click="RotationToggle_Click" Content="Toggle Rotation"/>
                <Button Margin="10" Click="ScaleToggle_Click" Content="Toggle Scale"/>
                <Button Margin="10" Click="InsetClipToggle_Click" Content="Toggle InsetClip"/>
            </StackPanel>
        </Grid>
    
    </Grid>
</Page>
