<Page
    x:Class="ContinuitySample.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ContinuitySample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" NavigationCacheMode="Enabled">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="FruitsList" 
                  ItemsSource="{x:Bind _sampleCollection}"
                  IsItemClickEnabled="True" 
                  ItemClick="FruitsList_ItemClick" 
                  ContainerContentChanging="FruitsList_ContainerContentChanging"
                  Loaded="FruitsList_Loaded">
            <ListView.ItemTemplate>
                <DataTemplate x:Name="NewsListTemplate">
                    <StackPanel Orientation="Horizontal" x:Name="ContentStackPanel">
                        <Rectangle x:Name="imageItem" Height="40" Width="40" Margin="0,2,2,2"/>
                        <TextBlock Text="{Binding Title}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
